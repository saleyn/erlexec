name: build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  OTP_LATEST: "29"

jobs:
  linux:
    runs-on: ubuntu-latest
    name: OTP ${{ matrix.otp }} ${{ matrix.os }}.${{ matrix.compiler.compiler }}

    container:
      image: erlang:${{matrix.otp}}

    strategy:
      matrix:
        os: [ linux ]
        compiler:
          - { compiler: GNU-14,  CC: gcc-14,      CXX: g++-14 }
          - { compiler: GNU-12,  CC: gcc-12,      CXX: g++-12 }
          - { compiler: LLVM-18, CC: clang-18,    CXX: clang++-18 }
          - { compiler: LLVM-16, CC: clang-16,    CXX: clang++-16 }
        otp: [27, 28, 29]
        exclude:
          # GCC 14 and clang 18 are only available in OTP 28 (Debian Bookworm+)
          - { otp: 27, compiler: { compiler: GNU-14 } }
          - { otp: 27, compiler: { compiler: LLVM-18 } }
          # GCC 12 and clang 16 are available in OTP 27 (Debian Bookworm)
          - { otp: 28, compiler: { compiler: GNU-12 } }
          - { otp: 28, compiler: { compiler: LLVM-16 } }
          # GCC 12 and clang 16 are available in OTP 27 (Debian Bookworm)
          - { otp: 29, compiler: { compiler: GNU-12 } }
          - { otp: 29, compiler: { compiler: LLVM-16 } }

    steps:
      - uses: actions/checkout@v2
      - name: OTP Version
        run:  erl -eval '{ok, Version} = file:read_file(filename:join([code:root_dir(), "releases", erlang:system_info(otp_release), "OTP_VERSION"])), io:format("Version=~s\n", [Version]), halt().' -noshell -noinput;
      - name: Fix Ownership
        run:  git config --global --add safe.directory ${GITHUB_WORKSPACE}
      - name: Install compiler
        run:  |
          apt-get update -qq
          apt-get install -y --no-install-recommends ${{ matrix.compiler.CC }} ${{ matrix.compiler.CXX }}
      - name: Build
        env:
          CC:  ${{ matrix.compiler.CC }}
          CXX: ${{ matrix.compiler.CXX }}
        run:  |
          make info
          make
      - name: Test
        env:
          CC:  ${{ matrix.compiler.CC }}
          CXX: ${{ matrix.compiler.CXX }}
        run:  |
          adduser --disabled-password --gecos '' newuser
          chown -R newuser:newuser .
          su -c "make test" newuser

  macOS:
    name: OTP ${{ matrix.otp }} macos.${{ matrix.compiler.compiler }}
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        compiler:
          - { compiler: XCode, CC: cc, CXX: c++ }
        otp: [27, 28, 29]
    steps:
      - uses: actions/checkout@v2
      - name: Install Erlang/OTP
        uses: erlef/setup-beam@v1
        with:
          otp-version: ${{ matrix.otp }}
          rebar3-version: '3'
      - name: OTP Version
        run:  erl -eval '{ok, Version} = file:read_file(filename:join([code:root_dir(), "releases", erlang:system_info(otp_release), "OTP_VERSION"])), io:format("Version=~s\n", [Version]), halt().' -noshell -noinput;
      - name: Fix Ownership
        run:  git config --global --add safe.directory ${GITHUB_WORKSPACE}
      - name: Build
        env:
          CC:  ${{ matrix.compiler.CC }}
          CXX: ${{ matrix.compiler.CXX }}
        run:  |
          make info
          make
      - name: Test
        env:
          CC:  ${{ matrix.compiler.CC }}
          CXX: ${{ matrix.compiler.CXX }}
        run:  make test
      - name: Docs
        env:
          CC:  ${{ matrix.compiler.CC }}
          CXX: ${{ matrix.compiler.CXX }}
        run:  make doc

#  windows-msys2:
#    name: OTP ${{ matrix.otp }} ${{ matrix.os }}.${{ matrix.msys2.msystem }}.${{ matrix.compiler.compiler }}
#    runs-on: ${{ matrix.os }}
#    strategy:
#      fail-fast: false
#      matrix:
#        os: [ windows-latest ]
#        msys2:
#          - { msystem: MINGW64, arch: x86_64 }
#        compiler:
#          - { compiler: GNU,  CC: gcc,   CXX: g++ }
#          - { compiler: LLVM, CC: clang, CXX: clang++ }
#        otp: ["24.1", "24.2"]
#    defaults:
#      run:
#        shell: msys2 {0}
#    steps:
#      - uses: actions/checkout@v2
#      - uses: msys2/setup-msys2@v2
#        with:
#          msystem: ${{ matrix.msys2.msystem }}
#          install: |
#            pacman -Ss bash curl git base-devel mingw-w64-${{ matrix.msys2.arch }}-toolchain mingw-w64-${{ matrix.msys2.arch }}-gcc mingw-w64-${{ matrix.msys2.arch }}-clang mingw-w64-${{ matrix.msys2.arch }}-cmake mingw-w64-${{ matrix.msys2.arch }}-ninja mingw-w64-${{ matrix.msys2.arch }}-zlib erlang rebar3
#          update: true
#      - name: Build And Test
#        env:
#          CC: ${{ matrix.compiler.CC }}
#          CXX: ${{ matrix.compiler.CXX }}
#          SRC_DIR: ${{ github.workspace }}
#          BUILD_DIR: ${{ github.workspace }}/build
#          INSTALL_PREFIX: ${{ github.workspace }}/install
#        run: |
#          erl -eval '{ok, Version} = file:read_file(filename:join([code:root_dir(), "releases", erlang:system_info(otp_release), "OTP_VERSION"])), io:format("Version=~s\n", [Version]), halt().' -noshell;
#          make
#
  create-tag:
    name: Create Tag if Version Changed
    runs-on: ubuntu-latest
    needs: [linux, macOS]
    if: github.event_name == 'push' && (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main')

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get project version
        id: project_version
        run: |
          VERSION=$(sed -nE 's/.*\{vsn,\s*"([^"]+)".*/\1/p' src/erlexec.app.src | head -1)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Project version: $VERSION"

      - name: Check if tag should be created
        id: should_create_tag
        run: |
          PROJECT_VERSION="${{ steps.project_version.outputs.version }}"
          MAX_TAG=$(git tag -l "*.*.*" | sed 's/^v//' | sort -V | tail -1)

          if [ -z "$MAX_TAG" ]; then
            echo "create_tag=true" >> $GITHUB_OUTPUT
            echo "First tag: $PROJECT_VERSION"
          else
            if [ "$(printf '%s\n' "$MAX_TAG" "$PROJECT_VERSION" | sort -V | tail -1)" = "$PROJECT_VERSION" ] && [ "$MAX_TAG" != "$PROJECT_VERSION" ]; then
              echo "create_tag=true" >> $GITHUB_OUTPUT
              echo "Project version $PROJECT_VERSION is greater than max tag $MAX_TAG, creating tag"
            else
              echo "create_tag=false" >> $GITHUB_OUTPUT
              echo "Project version $PROJECT_VERSION is not greater than max tag $MAX_TAG, skipping tag creation"
            fi
          fi

      - name: Create and push tag
        if: steps.should_create_tag.outputs.create_tag == 'true'
        run: |
          VERSION="${{ steps.project_version.outputs.version }}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "$VERSION"
          git push https://x-access-token:${{ secrets.RELEASE_PAT }}@github.com/${{ github.repository }}.git "$VERSION"

      - name: Trigger release workflow
        if: steps.should_create_tag.outputs.create_tag == 'true'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.RELEASE_PAT }}
          script: |
            // Explicitly trigger the release workflow via the GitHub API.
            // GitHub Actions has a limitation: workflows triggered by other workflows don't
            // automatically trigger additional workflows, even with a PAT. The release.yaml
            // workflow has workflow_dispatch enabled to allow manual triggering via the API.
            const version = "${{ steps.project_version.outputs.version }}";
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'release.yaml',
              ref: version,
              inputs: {}
            });
